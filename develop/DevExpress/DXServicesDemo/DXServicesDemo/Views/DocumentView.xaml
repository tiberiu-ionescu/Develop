<UserControl x:Class="DXServicesDemo.Views.DocumentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Width="400" Height="300" Background="Bisque">
        <TextBlock Text="Document View" FontSize="32" FontWeight="Bold" Opacity="0.5"
                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <StackPanel>
            <Button Content="MessageBox" Command="{Binding MessageCommand}"/>
            <Button Content="Dialog" Command="{Binding DialogCommand}"/>
            <Button Content="Document" Command="{Binding DocumentCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>